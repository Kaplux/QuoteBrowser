<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="fr.quoteBrowser"
    android:versionCode="1"
    android:versionName="1.0"
    android:debuggable="true"
     >

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.READ_PHONE_STATE" />
    <uses-sdk android:targetSdkVersion="8" android:minSdkVersion="8"/>

    <application
        android:icon="@drawable/icon"
        android:label="@string/app_name"
        android:theme="@android:style/Theme.Light" >
        <activity android:name=".activity.BrowseQuotesActivity" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".activity.QuotePreferencesActivity"></activity>
        <service android:name=".service.QuoteIndexationService"></service>
        <receiver android:name=".service.PeriodicalQuoteUpdater"></receiver>
    </application>

</manifest>